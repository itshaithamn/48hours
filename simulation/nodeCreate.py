class Node:
    def __init__(self, type, size, diameter, material, length):
        self.type = type
        self.size = size
        self.diameter = diameter
        self.material = material
        self.length = length

# We need the front-end to communicate to the back-end so we have all the data that we need
